# Copie este arquivo para .env.local e preencha com sua chave real do Resend
# (https://resend.com/api-keys). Nunca commite o .env.local — ele já está no
# .gitignore. Na Vercel, configure a mesma variável em
# Project Settings > Environment Variables.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
